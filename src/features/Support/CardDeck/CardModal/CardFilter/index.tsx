import { Box } from '@chakra-ui/react';
import React from 'react';

const CardFilter: React.FC = () => {
  return (
    <Box my={4} p={4} bgColor='white' borderRadius='16px'>
      card filter
    </Box>
  );
};

export default CardFilter;
